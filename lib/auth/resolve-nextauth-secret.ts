/**
 * @deprecated Import from `@/lib/auth/nextauth-env` instead.
 * Compatibility re-export — remove after remaining call sites migrate.
 */
export { resolveNextAuthSecret } from "@/lib/auth/nextauth-env";
