# CodeRabbit #388 findings — disposition (public informational GO gate)

**Source PR:** #388 (merged). Threads re-inspected against `origin/main` @ `2042a210` and this remediation branch.  
**Do not mark GitHub conversations resolved until this branch is pushed and reviewable.**

| #   | Finding                                     | Still applicable? | Disposition                      | Owning fix                                                                                     | Regression test                                                                       | Proposed reply                                                                                                                                                                                                    |
| --- | ------------------------------------------- | ----------------- | -------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A   | CSP report schema validation (Zod / bounds) | Yes               | Fixed                            | `lib/security/csp-report-schema.ts`, `app/api/security/csp-report/route.ts`                    | `tests/security/csp-report-schema.test.ts`, `tests/security/csp-report-route.test.ts` | Validated: Zod at the API boundary with string/array/object caps; malformed payloads return `400` `{error:"invalid_report"}` with no body echo; redaction unchanged.                                              |
| B   | Early Content-Length rejection              | Yes               | Fixed                            | `app/api/security/csp-report/route.ts`                                                         | oversized / malformed / zero / missing CL cases in `csp-report-route.test.ts`         | Validated: conservative CL check before buffer; dishonest/missing CL still bounded by post-read `MAX_BODY_BYTES`; `413`/`400` as appropriate.                                                                     |
| C   | Correlation-ID propagation to downstream    | Yes               | Fixed                            | `lib/security/forward-request-headers.ts`, `middleware.ts`                                     | `tests/security/request-correlation.test.ts`                                          | Validated: incoming IDs are validated via `resolveCorrelationId`; only the resolved ID is set on forwarded request headers and response; invalid IDs are replaced (no header injection).                          |
| D   | Malformed human-evidence JSON crash         | Yes               | Fixed                            | `scripts/ci/validate-human-release-evidence-readonly.mjs`                                      | `tests/ci/validate-human-release-evidence.test.ts`                                    | Validated: parse/stat/size/schema errors fail closed (`FAILED`) without throwing; incomplete schema remains `OWNER_ACTION_REQUIRED`.                                                                              |
| E   | Stale readiness / closure documentation     | Yes               | Fixed                            | `HEALTH_ENDPOINT_DIAGNOSIS.md`, `OWNER_ACTION_REQUIRED_OPS.md`, `CLOSURE_RESCAN_2026-07-22.md` | N/A (docs); readiness scripts still distinguish evidence classes                      | Validated: docs now cite main `2042a210`, failed Production `dpl_D6eih3Nn…` (HTTP env), live drift `dpl_MBD4G6…`, and do not claim pilot GO.                                                                      |
| F   | Apex health still FAILED / redeploy         | Yes (ops)         | Documented — not a missing route | Routes already on main; Production build blocked by env                                        | `tests/api/health-probes.test.ts` (repo)                                              | Acknowledged: repository routes `VERIFIED`; apex 404 is deployment drift + failed Production build on insecure `NEXTAUTH_URL`/`NEXT_PUBLIC_APP_URL`. Owner must set HTTPS apex URLs and redeploy remediation SHA. |
| G   | Owner evidence integrity                    | Yes (ops)         | Documented                       | Owner pack updated; validators fail-closed                                                     | human-evidence + deploy-evidence validators                                           | Acknowledged: agent cannot invent human/production evidence. Remaining gates stay `OWNER_ACTION_REQUIRED` / `NOT_RUN` until owner artefacts exist.                                                                |
