import type { MapAbleRequestClient } from "../client";

/**
 * Venue data module.
 * Placeholder for partner-facing venue APIs (details, floors, amenities).
 */
export class VenuesModule {
  constructor(private readonly client: MapAbleRequestClient) {}
}
